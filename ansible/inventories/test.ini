# Placeholder inventory for MVP structure.
# Replace with real internal hosts / groups on the Ansible control server.
# Never commit production secrets or private keys.

[all:vars]
ansible_user=ansible
ansible_connection=ssh

[linux_test]
# example-host ansible_host=10.0.0.10

[linux_test:vars]
environment=test
