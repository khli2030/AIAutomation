---
# Placeholder playbook — implement safely in a later phase.

- name: Set /dev/shm noexec
  hosts: all
  become: true
  serial: 20
  gather_facts: false
  tasks:
    - name: Placeholder — not implemented
      ansible.builtin.debug:
        msg: "Playbook stub for SET_DEV_SHM_NOEXEC"
