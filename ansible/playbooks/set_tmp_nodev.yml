---
# Placeholder playbook — implement safely in a later phase.
# fstab/mount changes require backup + human approval.

- name: Set /tmp nodev
  hosts: all
  become: true
  serial: 20
  gather_facts: false
  tasks:
    - name: Placeholder — not implemented
      ansible.builtin.debug:
        msg: "Playbook stub for SET_TMP_NODEV"
