---
# Placeholder playbook — implement safely in a later phase.

- name: Set /dev/shm nodev
  hosts: all
  become: true
  serial: 20
  gather_facts: false
  tasks:
    - name: Placeholder — not implemented
      ansible.builtin.debug:
        msg: "Playbook stub for SET_DEV_SHM_NODEV"
