---
# Phase 9A catalog playbook stub — implement safely in a later phase.
# Must never be generated from Excel Remediation text or AI drafts.
# Expected: net.ipv4.conf.all.secure_redirects = 0

- name: Disable IPv4 secure redirects
  hosts: all
  become: true
  serial: 20
  gather_facts: false
  tasks:
    - name: Placeholder — not implemented
      ansible.builtin.debug:
        msg: "Playbook stub for SYSCTL_IPV4_SECURE_REDIRECTS_DISABLE"
