# Frontend local env (copy to .env.local — never commit secrets)

# Browser-facing backend URL (loopback only for lab)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional lab-only token for local convenience. Prefer Settings → sessionStorage.
# NEVER commit a real ADMIN_TOKEN.
# NEXT_PUBLIC_ADMIN_TOKEN=
